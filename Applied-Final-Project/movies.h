#ifndef MOVIES_H
#define MOVIES_H

#include "vectorc.h"
#include "events.h"
#include <QPixmap>

// Declare the movies vector and event instances
extern vectorC<Events> movies;
// extern QPixmap www;
// extern QPixmap opp;
// extern QPixmap bar;
// extern QPixmap morg;

extern Events* event1;
extern Events* event2;
extern Events* event3;
extern Events* event4;
extern Events* event6;
extern Events* event7;

#endif // MOVIES_H
